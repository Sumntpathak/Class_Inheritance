 class Shape{ //defining a class
        constructor(color){
          this.color = color;
        }
        drawShape(){ //parent class method
          document.write("The shape is Created);
        }
        calculateArea(){
          }
        }
